FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml .
RUN pip install --no-cache-dir -e ".[dev]"
COPY src/ src/
COPY .env.example .env
CMD ["sh", "-c", "uvicorn src.api.main:app --host 0.0.0.0 --port 8000 & chainlit run src/ui/app.py -h 0.0.0.0 -p 7860"]
